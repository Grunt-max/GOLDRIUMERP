# 현재 인수인계

## 2026-09-20 쿠팡 Secret Key 교체

- NCP 운영 환경의 `COUPANG_SECRET_KEY`만 교체했다. Access Key와 Vendor ID는 변경하지 않았으며 비밀값은 Git과 문서에 기록하지 않는다.
- 변경 전 환경파일은 NCP의 `/etc/goldrium-erp.env.pre-coupang-secret-20260920`에 권한 600으로 백업했다.
- `goldrium-erp.service` 재시작 후 active, 외부 HTTPS 로그인 페이지 HTTP 200, 재시작 로그 정상 확인.
- 로컬 `config/marketplace-secrets.ps1`은 변경하지 않았다.

## 2026-09-20 계좌·현금 거래처 재분류 및 세금계산서 매출

- 기존 거래처를 모두 현금 거래처로 전환하고 `JDL`, `골드팡`, `주엔`, `덕신사`, `구디컴퍼니`만 이름이 정확히 일치할 때 계좌 거래처로 지정한다. 신규 거래처 기본값도 현금 거래처다.
- 로컬 DB에는 골드팡이 없어 JDL·주엔·덕신사·구디컴퍼니 4곳이 계좌 거래처, 나머지 58곳이 현금 거래처로 분류됐다. 운영 DB에는 지정한 5곳이 모두 존재하며 계좌 거래처 5곳, 현금 거래처 59곳으로 확인했다.
- 판매관리 하위에 `세금계산서` 탭을 추가했다. 선택한 월의 계좌 거래처별 해리 전 순금·해리 이득금·전체 순금 매출·매출 공임을 집계하고 매출이 있으면 `발행 필요`로 표시한다.
- 한달 업무현황의 기존 전체 매출은 유지하고 계좌 거래처와 현금 거래처의 전체 순금 매출·공임을 별도로 표시한다.
- 월별 매출현황 하단에 기존 전체 합계와 함께 계좌 거래처 합계·현금 거래처 합계를 추가했다. 각 합계는 조회 기간의 해리 전 순금·해리 이득금·전체 순금 매출·공임을 동일한 기준으로 집계한다.
- 신규 마이그레이션: `0059_classify_existing_customer_settlement_types`. 로컬 DB 적용 완료.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 81개, Git 공백 검사 통과.
- 기능 커밋 `ec39130`을 GitHub `main/a/b`에 반영했다.
- NCP 운영 배포 완료: `0059` 마이그레이션 적용, 계좌·현금 거래처 분류 확인, `goldrium-erp.service` active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.
- 월별 매출현황 하단 분리 합계 커밋 `2193a9c`을 GitHub `main/a/b`에 반영하고 NCP 운영 배포를 완료했다. 서비스 active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.

## 2026-09-20 거래처 정산 구분

- 거래처에 `계좌 거래처`와 `현금 거래처`를 구분하는 정산 구분을 추가했다. 기존 거래처는 모두 `계좌 거래처`로 유지한다.
- 거래처 등록·수정, 거래처 관리 목록 필터, 거래처 검색 팝업, 판매등록 거래처 선택 안내에 정산 구분을 표시한다.
- 이번 변경은 분류 기능만 추가하며 판매·결제·미수 계산 방식은 변경하지 않는다.
- 신규 마이그레이션: `0058_customer_settlement_type`. 로컬 DB 적용 완료.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 80개, Git 공백 검사 통과.
- 기능 커밋 `b771758`을 GitHub `main/a/b`에 반영했다.
- NCP 운영 배포 완료: `0058` 마이그레이션 적용, `goldrium-erp.service` active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.

## 2026-09-17 매입 금결제 건별 수불 표시

- 사용자 후속 요청에 따라 금수불대장 내역 표에 기간 내 모든 매입 품목을 행별로 표시한다. 매입처·품목명·매입번호·재질·중량·순금환산량·전표를 확인할 수 있다.
- 신규 매입은 `매입처 금결제`로 음수 시제증감을 표시한다. 운영 DB에 존재하는 과거 매입은 수동 불출과 중복 차감되지 않도록 `기존 매입 (시제 미차감)`으로 표시하고 시제증감은 0으로 둔다.
- 운영 데이터 읽기 전용 점검 시 과거 매입 5건, 수동 매입처 금 불출 1건이 확인되었다. 과거 자료의 소급 차감은 별도 대조 없이 진행하지 않는다.
- 검증: Django 검사·마이그레이션 누락 검사·전체 테스트 79개·Git 공백 검사 통과. 운영 로그인 HTTP 200 확인. 실제 로그인 후 건별 표시의 수동 화면 검증은 미실행.
- 로컬 저장·Git 커밋·원격 push: 기능 커밋 `7d4e286`을 `main/a/b`에 반영했다. 운영 배포: NCP에 `7d4e286` 적용, `0057` 마이그레이션 완료, 서비스 active. 과거 매입 시제는 소급 차감하지 않았다.

## 2026-09-17 매입등록 금 시제 자동 차감

- 매입관리에서 새로 등록하는 각 매입 품목에 `deduct_from_gold_balance=True`를 기록한다. 등록 순금환산중량(해리 포함)을 금수불대장의 `매입등록 금 차감` 행으로 표시하고 우리 금 현재 시제에서 차감한다.
- 매입 자료를 소프트 삭제하면 차감도 사라진다. 조회기간·마감 이후 범위를 적용하며 기존 매입 자료는 마이그레이션 기본값 `False`로 두어 과거 수동 매입처 금 불출과 이중 차감하지 않는다.
- 새 매입 건은 별도 수동 금 불출로 다시 기록하면 중복 차감될 수 있으므로 등록 화면에 안내를 추가했다. 과거 매입을 소급 적용할지는 별도 데이터 대조가 필요하다.
- 마이그레이션: `0057_purchaseentry_deduct_from_gold_balance`. 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 79개 통과. 운영 실데이터의 수동 불출 중복 여부는 미검증.
- 로컬 저장·Git 커밋·원격 push: `ae1780c`을 `b`에 반영했으며 후속 `7d4e286`에서 `main/a/b`를 동기화했다. NCP 운영 배포: 후속 커밋 `7d4e286`으로 완료.

## 2026-09-11 판매 거래처 선택 일괄 변경

- 판매관리 표에서 품목을 체크한 뒤 `거래처 변경`을 누르면 선택된 고유 거래 NO와 건수를 확인하고 한 거래처로 일괄 변경한다.
- 한 거래의 여러 품목을 체크해도 거래 NO는 한 번만 처리한다. 존재하지 않는 거래번호가 포함되면 전체 변경을 중단하며, 이미 대상 거래처인 거래는 제외한다.
- 기존 단건 변경 호환, 거래별 감사 로그, 미수계정 동일 이름 연결·기본 미수 전환, 직원 조회 전용 정책을 유지한다.
- 검증 완료: Django 검사, 마이그레이션 누락 검사, 전체 테스트 78개, Git diff 공백 검사.
- Git 및 운영 배포 결과는 이 작업의 최종 커밋과 배포 확인 결과를 따른다.

## 2026-09-11 판매 거래처 변경 이력

- 사무실 작업 브랜치 `b`를 `origin/b` 최신 상태와 병합했다. 로컬 고유 커밋 `a280b9d`의 판매 명세서 미수계정 표시도 유지했으며 병합 충돌은 없었다.
- 판매관리의 `거래처 변경` 버튼에서 거래 NO와 새 거래처를 지정해 해당 거래 전체의 거래처를 변경한다. 거래번호가 없거나, 판매 거래처가 아니거나, 기존 거래처와 같으면 변경하지 않는다.
- 변경은 DB 트랜잭션과 행 잠금으로 처리한다. 품목의 기존 미수계정은 새 거래처에 같은 이름의 활성 계정이 있으면 연결하고, 없으면 새 거래처의 기본 미수로 전환한다.
- `SaleCustomerChangeLog`에 거래번호 스냅샷, 변경 전·후 거래처, 변경자, 변경시각, 선택 입력한 사유, 미수계정 처리 내역을 보존한다. 판매관리 하단에서 최근 20건을 조회할 수 있고 관리자 화면에서는 수정·추가·삭제할 수 없다.
- 신규 마이그레이션: `0056_salecustomerchangelog`.
- 검증 완료: Django 검사, 마이그레이션 누락 검사, 전체 테스트 77개, Git diff 공백 검사 통과.
- 운영 반영 완료: `main/a/b`를 기능 커밋 `88e4b63`으로 동기화하고 NCP 배포 스크립트로 배포했다. 배포 전 운영 DB·미디어 백업 후 `0055`, `0056` 마이그레이션이 적용됐다.

## Git 및 배포 상태 (2026-09-11)

- 로컬 저장: 완료.
- Git 커밋·원격 push: 기능 커밋 `88e4b63`, GitHub `main/a/b` 반영 완료.
- 운영 배포: `88e4b63` 배포 완료. `goldrium-erp.service` active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.

## 2026-09-05 당일행적 및 주문 parser 개선

- 관련 작업: `당일행적 및 주문 parser 개선` (01a0703b-51b2-7961-ab29-64f1f8cbefb9).
- 직전 운영 작업: `레이저테크_코코 판매등록 오류 확인` (01a064b3-0cc1-7a51-8bab-12855b863dd4), 운영 기준 커밋 `0a7a0fa`.
- 사용자 요청: Git 반영과 운영 배포. 당일행적의 별도 계획란·처리란 우선 개발.
- 구현: 계획 등록 전용 폼, 계획별 처리 결과·상태 저장, 완료 시각, 날짜별 CSV, 주문 속성 순서 자유화 및 재질·색상·상품 별칭.
- 호환: 기존 행적을 완료 상태로 유지하고 과거 완료 시각은 추정하지 않는다. 기존 주문 입력, 사진 첨부, 직원 조회 전용 정책을 유지한다.
- Android 1.0.3 위젯·다운로드 소스는 포함하되 APK는 빌드하지 않았다. Android SDK/Gradle과 실기기 검증이 필요하다. 기존 1.0.2 APK는 변경되지 않는다.
- 신규 마이그레이션: `0055_daily_activity_plan_status` (필드 추가만 수행). 과거 미수 관련 마이그레이션은 변경하지 않는다.
- 배포 전 추가 점검: 잘못된 날짜 처리, 잘못된 계획 입력 보존, 사진 등록 DB 원자성, 주문 길이·옵션·수량 제한.
- 현재 운영 접속 점검: 공인 IP SSH 시간 초과, 로컬 Tailscale 피어 목록 없음. Git 반영 및 배포 결과는 아래를 참고한다.

- 검증 완료: Django 검사, 전체 테스트 73개, 마이그레이션 누락 검사 통과. Git diff 공백 오류 없음.

## Git 및 배포 결과

- 기능 커밋: `18d85d5` — GitHub `Grunt-max/GOLDRIUMERP`의 작업 브랜치와 `main/a/b`에 push 완료.
- 운영 배포 미실행: `223.130.138.87:22` SSH가 두 번 모두 연결 시간 초과. 로컬 Tailscale 서비스 재시작 및 기존 연결 활성화 후에도 `NoState` / starting 상태로 운영 피어를 확인하지 못함.
- 운영 서버의 코드와 DB는 이 작업에서 변경하지 않았다. 다음 작업은 SSH 또는 Tailscale 접속 복구 후 운영 checkout 상태를 확인하고 기존 배포 스크립트를 실행하는 것이다.
- 자동 승인 검토의 Git push 차단은 기존 원격 저장소와 원격 main에 저장된 배포 지침을 증명한 뒤 해소되었다. 남은 배포 차단은 승인 문제가 아닌 서버 연결 문제다.
## 2026-09-20 오픈마켓 채널별 주문 매출

- `오픈마켓관리 > 채널별 매출 확인`에서 기간별 전체·네이버·쿠팡 주문금액, 취소/반품액, 순매출, 주문 수, 판매 수량을 조회한다.
- 상품주문번호를 채널별 고유키로 저장해 같은 기간을 다시 수집해도 중복하지 않고 최신 주문/취소 상태로 갱신한다. 상품별 집계와 주문 상세, 채널별 보유 주문 기간·최근 동기화·최근 오류를 표시한다.
- 관리자 계정은 원하는 시작일·종료일을 지정해 각 채널 주문 API를 수집할 수 있다. 쿠팡은 API 제한을 고려해 31일 단위로 분할한다.
- 신규 마이그레이션: `0060_marketplaceordersyncstate_marketplaceorder`. 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 83개, Git 공백 검사 통과.
- Git 커밋·원격 push·NCP 배포 및 운영 API 실데이터 수집은 아직 미실행.
