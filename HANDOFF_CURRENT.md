# 현재 인수인계

## 2026-09-26 상품등록 작업실 가독성 개편

- 운영 화면 캡처에서 라벨·도움말·입력칸이 서로 떨어져 보이고 옵션명과 옵션값을 반복 입력해야 하는 문제를 확인했다.
- 공통·네이버·쿠팡 폼의 각 필드를 `workspace-field` 단위로 묶어 라벨 바로 아래에 입력칸과 도움말이 보이도록 정렬했다. 마켓 등록정보는 `상품 기본정보 / A/S·원산지 / 배송·반품`으로 묶었다.
- 옵션명 1·2는 상단 `고객에게 보일 옵션명`에서 한 번만 입력하고, 아래 표에서는 조합별 옵션값·정상가·실제 판매가·재고를 입력한다. 추가 행에도 옵션명을 자동 복제하며 저장 전에 모든 행을 동기화한다.
- 옵션 표를 9개 핵심 열로 줄이고 내부 원가 연결·SKU를 하나의 관리정보 열로 묶었다. 데스크톱은 가로 스크롤을 최소화하고, 휴대폰은 옵션 조합별 카드로 표시한다.
- 이미 외부에 등록된 상품은 등록 경고·비활성 버튼을 반복하지 않고, `외부 등록 완료`·상품번호·중복 등록 잠금 안내를 하나의 상태 카드로 표시한다. 상품정보고시와 API JSON은 계속 고급 설정으로 접어 둔다.
- 데이터 구조·마켓 payload는 변경하지 않았고 화면과 입력 동기화만 개편했다. 추가 마이그레이션은 없다.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 96개, Git 공백 검사 통과. 기능 커밋 `bad9358`과 인수인계 커밋 `f4849f9`를 GitHub `a/b/main`에 반영했다. NCP 운영 배포 완료: 추가 마이그레이션 없이 `goldrium-erp.service` active, 기존 `0068` 적용 상태, 외부 HTTPS 로그인 HTTP 200을 확인했다.

## 2026-09-26 마켓별 정상가·판매가 및 채널 탭 편집

- 상품등록 작업실을 `공통 상품정보 / 네이버 스마트스토어 / 쿠팡` 탭으로 나눴다. 두 마켓을 좌우로 동시 노출하지 않고 각 채널의 등록정보·옵션·고시정보·등록 버튼을 하나의 흐름으로 보여 준다.
- 판매 옵션을 작은 표 형태로 바꾸고 필요한 행만 추가하도록 했다. 상품정보고시 JSON과 API 추가 속성, 상세페이지 HTML 직접 수정은 고급 설정으로 접었고 상세페이지는 미리보기를 우선 표시한다.
- `OpenMarketChannelOption.original_price`를 추가했다. 비우면 판매가와 같게 처리하며 정상가는 판매가보다 낮을 수 없다.
- 네이버는 사용자가 각 조합의 정상가·최종 판매가를 입력하면 ERP가 API의 기본 판매가·조합 옵션 추가금·판매자 즉시할인으로 변환한다. 네이버는 한 상품에 즉시할인액 하나를 적용하므로 모든 옵션의 `정상가 - 판매가`가 다르면 저장·등록 전 오류를 보여 준다.
- 쿠팡은 각 vendor item의 `originalPrice`와 `salePrice`를 옵션별로 독립 전송한다. 플랫폼 쿠폰·멤버십 혜택은 ERP 판매가와 섪지 않는다.
- 신규 마이그레이션: `0068_openmarketchanneloption_original_price`. 로컬 DB에 `0066`·`0067`·`0068` 적용 완료.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 95개, Git 공백 검사 통과. 로컬 브라우저 시각 검증은 브라우저 연결 런타임의 신뢰 경로 오류로 실행하지 못했으며 서버 렌더링과 행동 테스트는 통과했다.
- 로컬 저장·Git 커밋: 기능 `1f8f229`, 인수인계 `c8b75fa` 완료. GitHub `a/b/main` 반영과 NCP 운영 배포 완료. 운영 `0068` 적용, `goldrium-erp.service` active, 외부 HTTPS 로그인 HTTP 200을 확인했다.

## 2026-09-26 내부 원가 기준과 마켓 판매 옵션 분리

- `OpenMarketVariant`의 14K·18K·S925 값은 중량·공임·마진과 권장가 계산을 위한 내부 원가 기준으로 유지한다. 더 이상 네이버·쿠팡 판매 옵션으로 자동 전송하지 않는다.
- 신규 `OpenMarketChannelOption`에서 마켓별 판매자 SKU, 최대 2단계 옵션명·옵션값, 실제 판매가, 재고, 사용 여부, 표시 순서를 독립 저장한다. 내부 원가 기준 연결은 선택사항이므로 네이버와 쿠팡의 옵션 및 가격을 서로 다르게 구성할 수 있다.
- 상품등록 작업실에 네이버·쿠팡별 판매 옵션 편집 영역을 추가했다. 등록 준비 검사, 내부 테스트 등록, 네이버 조합옵션 payload, 쿠팡 아이템 payload가 모두 각 채널 판매 옵션을 사용한다.
- 신규 마이그레이션: `0067_openmarketchanneloption`. 기존 상품에는 채널 판매 옵션을 임의 생성하지 않으므로 운영 반영 후 사용자가 마켓별로 직접 입력해야 등록 버튼이 활성화된다.
- 검증: Django 전체 테스트 92개 통과. 아직 Git 커밋·원격 push·운영 배포는 하지 않았다.

## 2026-09-20 작업실 마켓 등록정보 직접 입력

- GPT 상품등록 작업실 안에 네이버·쿠팡별 카테고리, 채널 상품명, 배송 방식·택배사, 출고지·반품지, 배송비, 상품정보고시와 추가 속성 입력란을 배치했다.
- 기존 상품에서 가져온 값은 참고용 초기값으로만 표시되며 사용자가 직접 수정하고 `전체 입력정보 저장`을 눌러 확정한다. 외부 등록 버튼은 저장된 값만 사용한다는 안내와 네이버 판매대기·쿠팡 승인요청 전 상태를 명시했다.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 90개, Git 공백 검사 통과.
- 네이버 첫 등록 400 응답에 따라 필수 A/S 정보, 원산지, 미성년자 구매 가능 여부와 원상품·채널상품 상태 입력란을 추가했다. 판매 시작 방지를 위해 기본값은 원상품 `SUSPENSION`, 채널 전시 `SUSPENSION`이며 스마트스토어 신규 등록에서 거절된 `WAIT`는 사용하지 않는다. 신규 마이그레이션은 `0066_openmarketchannelsetting_naver_required_fields`다.
- 필수정보가 부족할 때 등록 버튼이 사라지지 않고 비활성 상태와 부족한 항목을 함께 표시한다. 네이버·쿠팡 상품정보고시 상세와 고급 추가 속성은 작업실의 별도 7번 입력 탭으로 분리했다.
- 운영 ORO-362 점검 결과 네이버 상태·원산지·미성년자 값은 정상이며 A/S 전화번호와 안내가 비어 있었다. 화면의 기존 API 오류를 새 오류로 오해하지 않도록 `이전 등록 시도 오류`로 표시하고 채널 설정을 수정·저장하면 과거 실패 메시지와 실패 상태를 초기화한다.

## 2026-09-20 GPT 상품등록 작업실 1차

- `오픈마켓관리 > GPT 상품등록 작업실`을 추가했다. 새 상품 초안, 상품 기본정보, 대표 이미지, 이미지 수정 지시, 원가·마진·채널 수수료, GPT 문구 지시, 상세 설명, 등록 대상 채널과 검토 상태를 한 화면에서 저장한다.
- 기존 `OpenMarketProduct`를 작업실의 마스터 데이터로 그대로 사용한다. 새 초안을 처음 저장하면 14KY·14KP·18KY·18KP 기준 옵션과 선택한 네이버·쿠팡 채널 설정을 자동 생성한다.
- 작성 중·검토 대기·승인 완료·업로드 완료 상태별 목록과 검색을 제공한다. 저장 후 최신 확정 도매 금시세를 기준으로 네이버·쿠팡 권장 판매가를 표시한다.
- 신규 마이그레이션: `0063_openmarketproduct_workspace_fields`. 로컬 DB 적용 완료.
- 금 제품과 은 제품 가격 계산을 구분한다. 은 제품은 S925 단일 기준 옵션을 만들고 `은 중량 × 입력한 은 원가(원/g) + 공임`에 채널 수수료와 목표 마진을 적용한다. 신규 마이그레이션은 `0064_openmarketproduct_silver_pricing`이다.
- GPT API 호출, 이미지 자동 편집, 채널 실제 업로드는 아직 연결하지 않았다. 이번 단계에서는 필요한 지시와 승인 자료를 보존하는 작업실 기반을 구현했다.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 86개 통과. 작업실 기반 커밋은 `4766dcb`; 실버 가격 계산 후속 변경은 배포 결과를 따른다.

## 2026-09-20 GPT 상세페이지 및 채널 등록 연결

- OpenAI Responses API 구조화 출력으로 네이버용 상품명, 쿠팡용 상품명, 요약 설명과 긴 섹션형 상세페이지 HTML을 생성하는 기능을 추가했다. 운영 환경에는 아직 `OPENAI_API_KEY`, `OPENAI_PRODUCT_MODEL`이 없어 키 설정 전에는 생성 버튼이 설정 안내 오류를 표시한다.
- 네이버는 대표 이미지를 상품 이미지 API에 먼저 올린 뒤 `/external/v2/products`로 등록하며, 쿠팡은 `/v2/providers/seller_api/apis/api/v1/marketplace/seller-products`로 우선 승인요청 전 초안을 등록한다.
- 승인 완료, 대상 채널, 대표 이미지, 상세페이지, 카테고리, 계산 가능한 가격을 공통 필수조건으로 검사한다. 쿠팡은 출고지·반품지·고시정보도 필수다. 실제 등록은 master 재인증과 브라우저 확인을 거치고, 채널별 상품번호·상태·오류를 저장하며 중복 등록을 차단한다.
- 카테고리마다 요구하는 옵션·고시 필드가 다르므로 실제 첫 등록은 ORO-362의 채널 카테고리와 메타정보를 채운 뒤 API 오류 응답까지 확인해야 한다. 등록 실패 시 상품번호를 저장하지 않고 화면에 원문 오류를 남긴다.
- 신규 마이그레이션: `0065_openmarketchannelsetting_upload_tracking`. 전체 테스트 87개 통과.
- 후속으로 외부 API를 전혀 호출하지 않는 채널별 `내부 테스트 등록`을 추가했다. 승인 완료와 계산 가능한 옵션 가격만 확인한 뒤 `MarketplaceProduct`에 `TEST_PREVIEW` 상품번호로 저장하고 채널 아이템 조회에서 상품·가격·옵션을 확인한다. 실제 채널 상품번호 및 업로드 상태는 변경하지 않으며 테스트 자료만 삭제할 수 있다. 전체 테스트 88개 통과.
- 외부 판매대기/임시저장 테스트를 위해 기존 상품에서 복사한 네이버 배송지·반품지 같은 중첩 설정을 생성 payload에 병합하고, 네이버 조합옵션을 `optionCombinations` 구조로 수정했다. 쿠팡은 복사한 카테고리별 고시정보와 속성을 옵션에 반영한다. 전체 테스트 89개 통과.

## 2026-09-20 쿠팡 Secret Key 교체

- NCP 운영 환경의 `COUPANG_SECRET_KEY`만 교체했다. Access Key와 Vendor ID는 변경하지 않았으며 비밀값은 Git과 문서에 기록하지 않는다.
- 변경 전 환경파일은 NCP의 `/etc/goldrium-erp.env.pre-coupang-secret-20260920`에 권한 600으로 백업했다.
- `goldrium-erp.service` 재시작 후 active, 외부 HTTPS 로그인 페이지 HTTP 200, 재시작 로그 정상 확인.
- 로컬 `config/marketplace-secrets.ps1`은 변경하지 않았다.

## 2026-09-20 계좌·현금 거래처 재분류 및 세금계산서 매출

- 기존 거래처를 모두 현금 거래처로 전환하고 `JDL`, `골드팡`, `주엔`, `덕신사`, `구디컴퍼니`만 이름이 정확히 일치할 때 계좌 거래처로 지정한다. 신규 거래처 기본값도 현금 거래처다.
- 로컬 DB에는 골드팡이 없어 JDL·주엔·덕신사·구디컴퍼니 4곳이 계좌 거래처, 나머지 58곳이 현금 거래처로 분류됐다. 운영 DB에는 지정한 5곳이 모두 존재하며 계좌 거래처 5곳, 현금 거래처 59곳으로 확인했다.
- 판매관리 하위에 `세금계산서` 탭을 추가했다. 선택한 월의 계좌 거래처별 해리 전 순금·해리 이득금·전체 순금 매출·매출 공임을 집계하고 매출이 있으면 `발행 필요`로 표시한다.
- 한달 업무현황의 기존 전체 매출은 유지하고 계좌 거래처와 현금 거래처의 전체 순금 매출·공임을 별도로 표시한다.
- 월별 매출현황 하단에 기존 전체 합계와 함께 계좌 거래처 합계·현금 거래처 합계를 추가했다. 각 합계는 조회 기간의 해리 전 순금·해리 이득금·전체 순금 매출·공임을 동일한 기준으로 집계한다.
- 신규 마이그레이션: `0059_classify_existing_customer_settlement_types`. 로컬 DB 적용 완료.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 81개, Git 공백 검사 통과.
- 기능 커밋 `ec39130`을 GitHub `main/a/b`에 반영했다.
- NCP 운영 배포 완료: `0059` 마이그레이션 적용, 계좌·현금 거래처 분류 확인, `goldrium-erp.service` active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.
- 월별 매출현황 하단 분리 합계 커밋 `2193a9c`을 GitHub `main/a/b`에 반영하고 NCP 운영 배포를 완료했다. 서비스 active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.

## 2026-09-20 거래처 정산 구분

- 거래처에 `계좌 거래처`와 `현금 거래처`를 구분하는 정산 구분을 추가했다. 기존 거래처는 모두 `계좌 거래처`로 유지한다.
- 거래처 등록·수정, 거래처 관리 목록 필터, 거래처 검색 팝업, 판매등록 거래처 선택 안내에 정산 구분을 표시한다.
- 이번 변경은 분류 기능만 추가하며 판매·결제·미수 계산 방식은 변경하지 않는다.
- 신규 마이그레이션: `0058_customer_settlement_type`. 로컬 DB 적용 완료.
- 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 80개, Git 공백 검사 통과.
- 기능 커밋 `b771758`을 GitHub `main/a/b`에 반영했다.
- NCP 운영 배포 완료: `0058` 마이그레이션 적용, `goldrium-erp.service` active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.

## 2026-09-17 매입 금결제 건별 수불 표시

- 사용자 후속 요청에 따라 금수불대장 내역 표에 기간 내 모든 매입 품목을 행별로 표시한다. 매입처·품목명·매입번호·재질·중량·순금환산량·전표를 확인할 수 있다.
- 신규 매입은 `매입처 금결제`로 음수 시제증감을 표시한다. 운영 DB에 존재하는 과거 매입은 수동 불출과 중복 차감되지 않도록 `기존 매입 (시제 미차감)`으로 표시하고 시제증감은 0으로 둔다.
- 운영 데이터 읽기 전용 점검 시 과거 매입 5건, 수동 매입처 금 불출 1건이 확인되었다. 과거 자료의 소급 차감은 별도 대조 없이 진행하지 않는다.
- 검증: Django 검사·마이그레이션 누락 검사·전체 테스트 79개·Git 공백 검사 통과. 운영 로그인 HTTP 200 확인. 실제 로그인 후 건별 표시의 수동 화면 검증은 미실행.
- 로컬 저장·Git 커밋·원격 push: 기능 커밋 `7d4e286`을 `main/a/b`에 반영했다. 운영 배포: NCP에 `7d4e286` 적용, `0057` 마이그레이션 완료, 서비스 active. 과거 매입 시제는 소급 차감하지 않았다.

## 2026-09-17 매입등록 금 시제 자동 차감

- 매입관리에서 새로 등록하는 각 매입 품목에 `deduct_from_gold_balance=True`를 기록한다. 등록 순금환산중량(해리 포함)을 금수불대장의 `매입등록 금 차감` 행으로 표시하고 우리 금 현재 시제에서 차감한다.
- 매입 자료를 소프트 삭제하면 차감도 사라진다. 조회기간·마감 이후 범위를 적용하며 기존 매입 자료는 마이그레이션 기본값 `False`로 두어 과거 수동 매입처 금 불출과 이중 차감하지 않는다.
- 새 매입 건은 별도 수동 금 불출로 다시 기록하면 중복 차감될 수 있으므로 등록 화면에 안내를 추가했다. 과거 매입을 소급 적용할지는 별도 데이터 대조가 필요하다.
- 마이그레이션: `0057_purchaseentry_deduct_from_gold_balance`. 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 79개 통과. 운영 실데이터의 수동 불출 중복 여부는 미검증.
- 로컬 저장·Git 커밋·원격 push: `ae1780c`을 `b`에 반영했으며 후속 `7d4e286`에서 `main/a/b`를 동기화했다. NCP 운영 배포: 후속 커밋 `7d4e286`으로 완료.

## 2026-09-11 판매 거래처 선택 일괄 변경

- 판매관리 표에서 품목을 체크한 뒤 `거래처 변경`을 누르면 선택된 고유 거래 NO와 건수를 확인하고 한 거래처로 일괄 변경한다.
- 한 거래의 여러 품목을 체크해도 거래 NO는 한 번만 처리한다. 존재하지 않는 거래번호가 포함되면 전체 변경을 중단하며, 이미 대상 거래처인 거래는 제외한다.
- 기존 단건 변경 호환, 거래별 감사 로그, 미수계정 동일 이름 연결·기본 미수 전환, 직원 조회 전용 정책을 유지한다.
- 검증 완료: Django 검사, 마이그레이션 누락 검사, 전체 테스트 78개, Git diff 공백 검사.
- Git 및 운영 배포 결과는 이 작업의 최종 커밋과 배포 확인 결과를 따른다.

## 2026-09-11 판매 거래처 변경 이력

- 사무실 작업 브랜치 `b`를 `origin/b` 최신 상태와 병합했다. 로컬 고유 커밋 `a280b9d`의 판매 명세서 미수계정 표시도 유지했으며 병합 충돌은 없었다.
- 판매관리의 `거래처 변경` 버튼에서 거래 NO와 새 거래처를 지정해 해당 거래 전체의 거래처를 변경한다. 거래번호가 없거나, 판매 거래처가 아니거나, 기존 거래처와 같으면 변경하지 않는다.
- 변경은 DB 트랜잭션과 행 잠금으로 처리한다. 품목의 기존 미수계정은 새 거래처에 같은 이름의 활성 계정이 있으면 연결하고, 없으면 새 거래처의 기본 미수로 전환한다.
- `SaleCustomerChangeLog`에 거래번호 스냅샷, 변경 전·후 거래처, 변경자, 변경시각, 선택 입력한 사유, 미수계정 처리 내역을 보존한다. 판매관리 하단에서 최근 20건을 조회할 수 있고 관리자 화면에서는 수정·추가·삭제할 수 없다.
- 신규 마이그레이션: `0056_salecustomerchangelog`.
- 검증 완료: Django 검사, 마이그레이션 누락 검사, 전체 테스트 77개, Git diff 공백 검사 통과.
- 운영 반영 완료: `main/a/b`를 기능 커밋 `88e4b63`으로 동기화하고 NCP 배포 스크립트로 배포했다. 배포 전 운영 DB·미디어 백업 후 `0055`, `0056` 마이그레이션이 적용됐다.

## Git 및 배포 상태 (2026-09-11)

- 로컬 저장: 완료.
- Git 커밋·원격 push: 기능 커밋 `88e4b63`, GitHub `main/a/b` 반영 완료.
- 운영 배포: `88e4b63` 배포 완료. `goldrium-erp.service` active, 외부 HTTPS 로그인 페이지 HTTP 200 확인.

## 2026-09-05 당일행적 및 주문 parser 개선

- 관련 작업: `당일행적 및 주문 parser 개선` (01a0703b-51b2-7961-ab29-64f1f8cbefb9).
- 직전 운영 작업: `레이저테크_코코 판매등록 오류 확인` (01a064b3-0cc1-7a51-8bab-12855b863dd4), 운영 기준 커밋 `0a7a0fa`.
- 사용자 요청: Git 반영과 운영 배포. 당일행적의 별도 계획란·처리란 우선 개발.
- 구현: 계획 등록 전용 폼, 계획별 처리 결과·상태 저장, 완료 시각, 날짜별 CSV, 주문 속성 순서 자유화 및 재질·색상·상품 별칭.
- 호환: 기존 행적을 완료 상태로 유지하고 과거 완료 시각은 추정하지 않는다. 기존 주문 입력, 사진 첨부, 직원 조회 전용 정책을 유지한다.
- Android 1.0.3 위젯·다운로드 소스는 포함하되 APK는 빌드하지 않았다. Android SDK/Gradle과 실기기 검증이 필요하다. 기존 1.0.2 APK는 변경되지 않는다.
- 신규 마이그레이션: `0055_daily_activity_plan_status` (필드 추가만 수행). 과거 미수 관련 마이그레이션은 변경하지 않는다.
- 배포 전 추가 점검: 잘못된 날짜 처리, 잘못된 계획 입력 보존, 사진 등록 DB 원자성, 주문 길이·옵션·수량 제한.
- 현재 운영 접속 점검: 공인 IP SSH 시간 초과, 로컬 Tailscale 피어 목록 없음. Git 반영 및 배포 결과는 아래를 참고한다.

- 검증 완료: Django 검사, 전체 테스트 73개, 마이그레이션 누락 검사 통과. Git diff 공백 오류 없음.

## Git 및 배포 결과

- 기능 커밋: `18d85d5` — GitHub `Grunt-max/GOLDRIUMERP`의 작업 브랜치와 `main/a/b`에 push 완료.
- 운영 배포 미실행: `223.130.138.87:22` SSH가 두 번 모두 연결 시간 초과. 로컬 Tailscale 서비스 재시작 및 기존 연결 활성화 후에도 `NoState` / starting 상태로 운영 피어를 확인하지 못함.
- 운영 서버의 코드와 DB는 이 작업에서 변경하지 않았다. 다음 작업은 SSH 또는 Tailscale 접속 복구 후 운영 checkout 상태를 확인하고 기존 배포 스크립트를 실행하는 것이다.
- 자동 승인 검토의 Git push 차단은 기존 원격 저장소와 원격 main에 저장된 배포 지침을 증명한 뒤 해소되었다. 남은 배포 차단은 승인 문제가 아닌 서버 연결 문제다.
## 2026-09-20 오픈마켓 채널별 주문 매출

- `오픈마켓관리 > 채널별 매출 확인`에서 기간별 전체·네이버·쿠팡 주문금액, 취소/반품액, 순매출, 주문 수, 판매 수량을 조회한다.
- 상품주문번호를 채널별 고유키로 저장해 같은 기간을 다시 수집해도 중복하지 않고 최신 주문/취소 상태로 갱신한다. 상품별 집계와 주문 상세, 채널별 보유 주문 기간·최근 동기화·최근 오류를 표시한다.
- 관리자 계정은 원하는 시작일·종료일을 지정해 각 채널 주문 API를 수집할 수 있다. 쿠팡은 API 제한을 고려해 31일 단위로 분할한다.
- 신규 마이그레이션: `0060_marketplaceordersyncstate_marketplaceorder`. 검증: Django 검사, 마이그레이션 누락 검사, 전체 테스트 83개, Git 공백 검사 통과.
- 기능 커밋 `be72362`를 GitHub `main/a/b`에 동기화하고 NCP에 배포했다. `0060` 적용, 서비스 active, 외부 HTTPS 로그인 HTTP 200을 확인했다. 운영 API 과거 주문 수집은 화면에서 기간을 선택해 실행하도록 남겨 두었다.
- 네이버 주문 API의 호출당 최대 24시간 제한에 맞춰 선택 기간을 KST 날짜별로 분할하고, 페이지가 남으면 모두 조회한다. 최근 180일보다 오래된 시작일은 자동 제한한다. 전체 테스트 84개 통과.
- 사용자 의도에 맞춰 채널별 매출 화면의 주 데이터 원천을 주문에서 정산 API로 전환했다. 네이버 일별 정산과 쿠팡 매출인식 상세를 공통 정산 구조로 저장해 판매·환불·수수료·실정산액을 표시한다. 신규 마이그레이션 `0061_marketplacesettlement`, 전체 테스트 84개 통과.
- 네이버 상품별 표시를 위해 건별 정산 API를 정산기준일별로 추가 수집한다. 상단 네이버 합계는 일별 정산 자료만 사용해 중복을 막고, 상품별·상세 표는 건별 자료만 사용한다.
- 쿠팡 상품 매출과 별도로 제공되는 주문별 배송비·배송비 수수료·배송비 정산액을 `SHIPPING` 정산 행으로 저장하고 화면에 배송비 매출을 별도 표시한다. 신규 마이그레이션 `0062_marketplacesettlement_record_type`.
