# 골드리움 ERP 목적 및 개발 기준

## 프로젝트 목적

1. 골드리움의 모든 업무를 한곳에서 관할한다. 주문·판매·매입·재고·금 수불·미수금과 일일 업무 계획·처리를 연결한다.
2. 오픈마켓 상품 업로드 및 가격 수정을 ERP에서 편리하게 수행한다.
3. 직원 교육에 활용한다. 업무별 사용법, 표준 처리 절차, 실습과 교육 이력으로 확장한다.

## 2026-09-05 추가 개발

### 당일행적 계획·처리

- 우선 개발 대상은 당일행적의 별도 계획란과 처리란이다. 위젯 확장보다 웹의 계획 등록 → 처리 기록 흐름을 먼저 완성한다.
- 계획 등록에서는 계획일·계획 내용·사진만 입력하며, 항상 계획 상태로 저장한다.
- 업무마다 계획란과 처리란을 나란히 표시한다. 휴대폰에서는 위아래로 표시한다. 처리란에 진행 내용·결과와 상태를 별도로 저장하며 원래 계획 내용은 유지한다.
- 대시보드에서도 별도 계획·처리 화면으로 진입한다.
- 상태와 처리 결과를 저장하고 완료 시각을 기록한다. 다시 진행 중으로 바꾸면 완료 시각을 비운다.
- 기존 행적은 완료로 유지하되, 과거의 실제 완료 시각은 추정하여 채우지 않는다.
- 기존 사진 첨부 기능을 이용한다. 선택한 날짜의 직접 기록한 업무 목록을 CSV로 다운로드한다. 사진 원본과 자동 집계된 판매·결제 내역은 CSV에 포함되지 않는다.
- 기존 권한 정책을 유지한다. master는 등록·처리가 가능하고, 직원은 허용된 메뉴를 조회·다운로드한다.

### 안드로이드 홈 화면 위젯

- 기존 골드리움 킹 앱에 ‘골드리움 당일행적’ 위젯을 추가한다.
- ‘계획·처리·사진 등록’은 당일행적 입력 위치를, ‘업무 목록·다운로드’는 당일행적 화면을 연다.
- 로그인된 앱에서 등록 및 다운로드한다. 위젯 자체에 업무 내용을 표시하거나 오프라인에서 저장하는 기능은 포함하지 않는다.
- 앱 내 다운로드는 로그인 세션을 전달한다. Android 10 이상은 다운로드 폴더에 저장하며, 이전 버전은 앱 전용 다운로드 폴더와 완료 알림을 이용한다.
- 새 APK 설치 후 홈 화면을 길게 눌러 위젯 목록에서 추가한다. 기존 APK에는 이 위젯이 없다.
- 접속 주소는 기존 앱 설정을 사용하며 Tailscale 연결이 필요하다.

### 간단주문 parser

- 한 줄에 한 상품을 입력한다. 재질·색상·모델·길이·완제품 수량은 순서에 관계없이 인식한다.
- 예: `코코체인 5미터 18K 베이지`, `2개 42cm 코코체인 로즈골드 14K / 연장고리`, `14kp 1.3mm로프 5M`.
- 색상 별칭: P/PG/pink/핑크/로즈골드, G/Y/YG/yellow/옐로우, W/WG/white/화이트, B/BG/beige/베이지 등.
- 재질: 14K/14케이, 18K/18케이, 24K/24케이/순금, 925 Silver/S925/실버 등. 해당 재질은 기초자료에서 활성 상태여야 한다.
- 상품에 등록된 여러 ProductAlias를 대소문자 구분 없이 실제 상품 코드로 연결한다.
- M/미터는 반제품 주문량, CM/센티미터는 완제품 길이로 해석한다. 완제품 수량은 `2개`, `x2`, `×2`, `2pcs`로 입력할 수 있다.
- `/` 뒤는 기존처럼 완제품 옵션이다. 반제품 옵션은 기존 처리와 동일하게 제외한다.
- 재질·색상이 충돌하거나 길이가 여러 개이면 잘못된 줄 번호를 알린다. 한 줄이라도 오류가 있으면 주문 전체를 저장하지 않는 기존 동작을 유지한다.

## 검증 및 배포

현재 검증 결과: Django 검사, 마이그레이션 누락 검사, 전체 테스트 73개 통과. 현재 환경에서 Android SDK와 Gradle을 찾지 못해 1.0.3 APK 빌드와 기기 검증은 미실행이다. 운영 서버 및 실제 DB에는 아직 적용하지 않았다.

- 웹 변경은 Django 검사와 전체 테스트를 실행한다. 순서 조합, 별칭, 오류 입력, 상태 전환, 다운로드와 직원 권한을 검증한다.
- 데이터 구조 변경은 `0055_daily_activity_plan_status` 마이그레이션으로 반영한다. 운영 반영 시 기존 배포 절차에 따라 백업 후 적용한다.
- 안드로이드 변경은 SDK·Gradle 환경에서 빌드하고 실제 기기에서 위젯 추가, 앱 재실행, 로그인, 사진 첨부, CSV 다운로드를 확인해야 한다.
- 오픈마켓 업로드·가격 수정과 직원 교육 전용 기능은 프로젝트의 지속 개발 범위이며, 이번 추가 개발에서 새로 구현한 기능은 아니다.

참고: [Android 위젯 문서](https://developer.android.com/develop/ui/views/appwidgets), [다운로드 API 문서](https://developer.android.com/reference/android/app/DownloadManager.Request).
